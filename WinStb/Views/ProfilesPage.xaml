<Page
    x:Class="WinStb.Views.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:WinStb.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Add Profile" Click="AddProfile_Click" />
            <TextBlock Text="{x:Bind MainViewModel.StatusMessage, Mode=OneWay}"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       FontStyle="Italic"/>
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind LocalViewModel.Profiles, Mode=OneWay}"
                  SelectedItem="{x:Bind LocalViewModel.SelectedProfile, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Profile">
                    <Grid Margin="0,10" Width="600">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{x:Bind PortalUrl}" Opacity="0.7" Margin="0,5,0,0"/>
                            <TextBlock Text="{x:Bind MacAddress}" Opacity="0.7"/>
                            <TextBlock Text="{x:Bind StbType}" Opacity="0.5" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="5">
                            <Button Content="Connect" Click="ConnectProfile_Click" Tag="{x:Bind Id}"/>
                            <Button Content="Edit" Click="EditProfile_Click" Tag="{x:Bind Id}"/>
                            <Button Content="Delete" Click="DeleteProfile_Click" Tag="{x:Bind Id}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressRing Grid.Row="1"
                      IsActive="{x:Bind LocalViewModel.IsLoading, Mode=OneWay}"
                      Width="50" Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</Page>
